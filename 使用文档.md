# holderAndAction 使用文档

## 快速上手（3步）

### Step 1：部署到阿里云

```bash
cd ~/Desktop/holderAndAction
bash deploy.sh 101.201.171.174 root
```

部署完成后，登录服务器补填 DeepSeek API Key：

```bash
ssh root@101.201.171.174
nano /opt/holder-action/.env
# 找到 DEEPSEEK_API_KEY=sk-your-deepseek-key，替换为真实 key
systemctl restart holder-action
```

### Step 2：开放阿里云安全组端口

阿里云控制台 → ECS → 安全组 → 入方向 → 添加规则：
- 端口：`8001`
- 来源：`0.0.0.0/0`

### Step 3：手机访问

浏览器打开：`http://101.201.171.174:8001/upload`

---

## 功能一：截图上传解析持仓

**场景**：你有富途/老虎/同花顺的持仓截图，想快速录入系统。

1. 手机浏览器打开 `http://101.201.171.174:8001/upload`
2. 点击「📷 点击选择截图」，从相册选择持仓截图
3. 点击「解析持仓截图」按钮，等待 3-5 秒
4. 页面显示识别结果，核对持仓数据是否正确
5. 点击「保存持仓到数据库」，数据入库

**支持的截图类型**：
- 富途牛牛持仓页
- 老虎证券持仓页
- 同花顺持仓页
- 雪球组合页面
- 其他显示：股票名称/代码/数量/成本价/现价/盈亏 的截图

**识别准确率提示**：
- 截图越清晰，识别越准
- 包含完整表格比局部截图效果好
- 如有识别错误，部署后可在 `/docs` 接口直接手动录入

---

## 功能二：获取调仓建议

**场景**：想让 AI 基于你的持仓 + 当前大模型信号，给出是否调仓的建议。

### 方式A：截图上传后直接获取（推荐）

1. 完成截图上传解析后，直接点击「获取调仓建议（DeepSeek R1）」
2. 等待 15-30 秒（R1 在深度推理）
3. 页面显示：
   - **总体概述**：当前市场环境和持仓健康度
   - **逐资产建议**：持有/加仓/减仓/平仓，附理由
   - **风险提示**：需要注意的风险点

**建议紧急程度说明**：
- 🔴 **urgent**（红色）：止损触及或信号反转，需立即处理
- 🟡 **normal**（黄色）：建议近期操作
- ⚪ **low**（灰色）：观察为主，暂不操作

### 方式B：API 调用（针对已录入持仓）

```bash
# 直接读数据库里的开仓持仓生成建议
curl -X POST http://101.201.171.174:8001/api/advisor/advice-from-db
```

---

## 功能三：查看 LLM 信号

**前提**：finance-analysis 系统需要先运行过分析（`outputs/` 目录有输出文件）。

```bash
# 查看所有资产最新信号
curl http://101.201.171.174:8001/api/signals

# 查看单资产信号（如 NVDA）
curl http://101.201.171.174:8001/api/signals/NVDA

# 触发重新分析（后台运行，30秒后刷新）
curl -X POST http://101.201.171.174:8001/api/signals/refresh/NVDA
```

---

## 功能四：持仓管理 API

### 手动新增持仓

```bash
curl -X POST http://101.201.171.174:8001/api/portfolio/positions \
  -H "Content-Type: application/json" \
  -d '{
    "asset": "NVDA",
    "ticker": "NVDA",
    "direction": "long",
    "entry_price": 178.5,
    "entry_date": "2026-04-01",
    "quantity": 10,
    "cost_basis_usd": 1785.0,
    "stop_loss": 160.0,
    "profit_target": 220.0
  }'
```

### 查看持仓（含实时 P&L）

```bash
curl http://101.201.171.174:8001/api/portfolio/positions
```

返回字段包含：
- `current_price`：实时价格
- `unrealized_pnl_usd`：浮盈亏（美元）
- `unrealized_pnl_pct`：浮盈亏（%）
- `position_status`：HOLD / STOP_TRIGGERED / TARGET_REACHED / SIGNAL_REVERSED
- `latest_signal_action`：最新 LLM 信号方向
- `latest_signal_bias`：信号置信度

### 平仓

```bash
curl -X POST http://101.201.171.174:8001/api/portfolio/positions/1/close \
  -H "Content-Type: application/json" \
  -d '{
    "exit_price": 210.0,
    "exit_date": "2026-04-19",
    "exit_reason": "take_profit"
  }'
```

---

## 功能五：仪表盘

```bash
# 持仓概览（总盈亏 + 告警）
curl http://101.201.171.174:8001/api/dashboard/summary

# 宏观数据（VIX/DXY/10Y收益率）
curl http://101.201.171.174:8001/api/dashboard/macro
```

---

## 费用参考

| 操作 | 模型 | 估算费用 |
|------|------|---------|
| 上传一张截图解析 | Qwen-VL Max | ~¥0.01 |
| 生成一次调仓建议 | DeepSeek R1 | ~¥0.05-0.2 |
| 查看信号/持仓 | 无 LLM | 免费 |

---

## 常见问题

**Q：截图上传后显示「未识别到持仓数据」**
- 确认截图包含完整持仓表格（不是局部截图）
- 试试截图更清晰/更高分辨率
- 部分截图有水印遮挡，尝试去掉水印后上传

**Q：调仓建议显示「生成建议失败」**
- 检查服务器 `.env` 中 `DEEPSEEK_API_KEY` 是否正确
- 运行 `systemctl status holder-action` 查看服务日志

**Q：信号显示「暂无分析结果」**
- finance-analysis 系统还没运行过对应资产的分析
- 在 finance-analysis 服务器上执行：`python3 tech_stock_analysis.py --ticker NVDA --api`

**Q：手机无法访问上传页面**
- 检查阿里云安全组是否开放了 8001 端口
- 确认 `systemctl status holder-action` 服务正在运行
